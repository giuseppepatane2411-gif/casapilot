export function idempotencyDecision(input:{requestId?:string;idempotencyKey?:string;payloadFingerprint?:string;previousFingerprint?:string;attempt:number;maxAttempts:number}){const blockers:string[]=[];if(!input.requestId)blockers.push("REQUEST_ID_REQUIRED");if(!input.idempotencyKey)blockers.push("IDEMPOTENCY_KEY_REQUIRED");if(!input.payloadFingerprint)blockers.push("PAYLOAD_FINGERPRINT_REQUIRED");if(input.previousFingerprint&&input.payloadFingerprint&&input.previousFingerprint!==input.payloadFingerprint)blockers.push("IDEMPOTENCY_COLLISION");if(input.attempt>input.maxAttempts)blockers.push("RETRY_EXHAUSTED");return{allowed:blockers.length===0,blockers,requiresHumanRecovery:blockers.includes("IDEMPOTENCY_COLLISION")||blockers.includes("RETRY_EXHAUSTED")}}
